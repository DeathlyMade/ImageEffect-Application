#ifndef ROTATION_H
#define ROTATION_H
#include "../Pixel.h"
#include "vector"
#include <cmath>
using namespace std;

vector< vector< Pixel>> applyRotation(vector< vector< Pixel>> imageVector, int angle);

#endif